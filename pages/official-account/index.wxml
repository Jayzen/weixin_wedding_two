<!-- 教程图片 -->
<view class="course">
    <image wx:for="{{official_accounts}}" mode="aspectFit" src="{{item.pic}}" />
</view>

<!-- 底部地址和电话 -->
<v-pagefooter phone="{{basic.phone}}" address="{{location.address}}" />
<v-support />
<v-loading class="loading-center" wx:if="{{loadingCenter}}" />