.logo{
  text-align:center;
  position: relative;
  margin:30rpx 0;
}
.logo text{
  display:inline-block;
  background:#fff;
  z-index: 99;
  padding:0 20rpx;
  font-size:14px;
  font-style: italic;
  font-weight:bold;
}
.logo text:before, .logo text:after{
  content:'';
  position: absolute;
  background:#000;
  height:1px;
  top:50%;
  /* right:0; */
  width:300rpx;
  z-index: -1;
}
.logo text:before{
  left:40rpx;
}
.logo text:after{
  right:40rpx;
}